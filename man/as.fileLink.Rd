% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/embedFile.R
\name{as.fileLink}
\alias{as.fileLink}
\title{Link to a self-contained embedded file}
\usage{
as.fileLink(path, label = NULL, type = NULL)
}
\arguments{
\item{path}{path to an existing file}

\item{label}{link text; defaults to the file name}

\item{type}{MIME type; guessed from the extension when \code{NULL}}
}
\value{
an object of class \code{c("html", "character")}
}
\description{
Turns a file into a download link that carries the file with it: the
contents travel base64-encoded inside the \code{href}, so the resulting
HTML needs no server and no accompanying assets. The counterpart of
\code{\link{as.img}} for non-image files -- a data set next to a table,
a script next to its output.
}
\details{
Browsers limit the size of a data URI, so this suits spreadsheets and
text files rather than large binaries.
}
\examples{
fn <- tempfile(fileext = ".csv")
write.csv(head(iris), fn, row.names = FALSE)
as.fileLink(fn, label = "iris")
unlink(fn)

}
\seealso{
Other html:
\code{\link[=as.html]{as.html()}},
\code{\link[=as.img]{as.img()}},
\code{\link[=embedFile]{embedFile()}},
\code{\link[=escapeHtml]{escapeHtml()}},
\code{\link{htmlNotation}},
\code{\link{htmlSubscript}},
\code{\link[=toHtmlTable]{toHtmlTable()}}
}
\concept{html}
