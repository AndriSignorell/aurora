% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pharos-graphics-framework.R, R/theme.R,
%   R/utils-plot.R
\name{graphics-framework}
\alias{graphics-framework}
\alias{.useTheme}
\alias{.drawGrid}
\alias{.drawBox}
\alias{.withGraphicsState}
\alias{.applyParFromDots}
\alias{.marTop}
\alias{.resolveTitle}
\alias{.marginLines}
\title{Graphics Framework Helpers}
\usage{
.useTheme

.drawGrid(grid, defaults = list())

.drawBox(box, defaults = list())

.withGraphicsState(expr, stamp = .useTheme, resetLayout = FALSE)

.applyParFromDots(..., exclude = "cex", defaults = list())

.marTop(main)

.resolveTitle(main, default = "")

.marginLines(
  labels,
  side = 4,
  las = par("las"),
  cex = par("cex"),
  pad = 0,
  axis.line = 0
)
}
\arguments{
\item{grid}{a callIf-style spec: the sentinel \code{.useTheme} (use the theme's
\code{grid} entry as on/off toggle), \code{TRUE} (draw with defaults), \code{FALSE} /
\code{NULL} / \code{NA} (suppress), or a named list of arguments passed on to
\code{\link[graphics:grid]{graphics::grid()}} (e.g. \code{list(nx = NA, ny = 5)}).}

\item{defaults}{named list of function-level default parameters
(tier 2).}

\item{box}{a callIf-style spec: \code{.useTheme} (theme decides), \code{TRUE}
(draw with defaults), \code{FALSE} / \code{NULL} / \code{NA} (suppress), or a named
list of arguments for \code{\link[graphics:box]{graphics::box()}} (e.g. \code{list(which = "figure")}).}

\item{expr}{the plot expression, evaluated in the caller's frame via
\code{eval.parent(substitute(expr))} so that promises and local variables
resolve as if the code ran inline.}

\item{stamp}{controls the stamp annotation drawn after a \emph{successful}
plot: the sentinel \code{.useTheme} (default; let the theme decide),
\code{TRUE}/\code{FALSE}/\code{NULL}/\code{NA} as an on/off toggle, a bare string or an
expression (used as the stamp text itself), or a list of arguments
for \code{\link[=stamp]{stamp()}} (e.g. \code{list(text = "...", las = 2)}).}

\item{resetLayout}{logical; if \code{TRUE}, the layout is reset to a single
panel (\code{layout(matrix(1))}) after successful completion. Use this in
plot functions that set up multi-panel layouts internally.}

\item{...}{user-supplied graphical parameters (the dots of the calling
plot function).}

\item{exclude}{character vector of parameter names that must never
reach \code{par()} from the defaults/dots tiers. Defaults to \code{"cex"}:
\code{cex} scales the line height and thus the margins, see the "cex
policy" in design_rules.md. The theme tier is deliberately exempt --
theme \code{cex} is global scaling, a different concern from the gated
function-argument \code{cex} (symbol size).}

\item{main}{the \code{main} argument as passed by the user.}

\item{default}{the function's default title, returned when
\code{main = NULL}.}

\item{labels}{character vector of labels to accommodate; \code{NULL} or
empty yields \code{0}.}

\item{side}{integer, the axis side (1 = bottom, 2 = left, 3 = top,
4 = right).}

\item{las}{label orientation as in \code{\link[=par]{par()}}; 2/3 = perpendicular to
the axis.}

\item{cex}{character expansion used for measuring; defaults to
\code{par("cex")}.}

\item{pad}{additional padding in lines.}

\item{axis.line}{offset of the axis labels from the plot region, in
lines.}
}
\value{
Invisibly \code{NULL}; called for its side effect (drawing).

Invisibly \code{NULL}; called for its side effect (drawing).

Invisibly \code{NULL}; called for its side effects.

Invisibly \code{NULL}; called for its side effect on \code{par()}.

A single numeric: \code{2.1} (no title) or \code{4.1} (title present).

A character string: \code{default}, \code{""}, or \code{main} itself.

A single numeric: the required margin size in lines (ceiling).
}
\description{
The internal machinery every plot function in the suite is built on:
state management, parameter resolution, title handling and the two
element dispatchers. They are exported so that plot functions living in
other packages of the suite - a \code{plot.FitMod()} in \pkg{alloy}, a
\verb{plot.Desc.*} method anywhere outside \pkg{pharos} - can follow the same
contract instead of reimplementing it.

They are not part of the user-facing API. Nothing here is meant to be
called from an analysis script, and the signatures may change with the
framework.\tabular{ll}{
   Helper \tab Purpose \cr
   \code{\link[=.withGraphicsState]{.withGraphicsState()}} \tab save/restore \code{par()}, draw the stamp on success \cr
   \code{\link[=.applyParFromDots]{.applyParFromDots()}} \tab apply graphical parameters from \code{...} over defaults \cr
   \code{\link[=.resolveTitle]{.resolveTitle()}} \tab three-state \code{main} contract (§9.4) \cr
   \code{\link[=.marTop]{.marTop()}} \tab top margin implied by the resolved title \cr
   \code{\link[=.marginLines]{.marginLines()}} \tab left margin implied by the drawn tick labels \cr
   \code{\link[=.drawGrid]{.drawGrid()}} \tab grid dispatcher, understands \code{.useTheme} \cr
   \code{\link[=.drawBox]{.drawBox()}} \tab box dispatcher, understands \code{.useTheme} \cr
   \code{.useTheme} \tab sentinel telling "resolve against the active theme" (§9.3) \cr
}

Resolves a callIf-style \code{grid} spec against the current theme and, unless
suppressed, draws a grid via \code{\link[graphics:grid]{graphics::grid()}}. This is the single
choke point for grid rendering in all plot functions, guaranteeing
consistent precedence: function defaults < theme settings < user spec.

Counterpart to \code{.drawGrid()} for \code{\link[graphics:box]{graphics::box()}}. Resolves a
callIf-style \code{box} spec against the current theme and draws the plot
box unless suppressed.

Runs \code{expr} (typically the body of a high-level plot function) while
guaranteeing that all commonly modified \code{\link[=par]{par()}} settings are restored
afterwards, and optionally places a stamp annotation and resets the
layout once the expression has completed successfully.

Central dispatcher that translates the three sources of graphical
parameters into \code{\link[=par]{par()}} calls, in strictly increasing precedence:

Returns the top margin in lines: compact (room for the axis only) when
no title will be drawn, generous otherwise. Centralizes the two values
as theme defaults instead of magic numbers at 40 call sites.

Normalizes the user-facing \code{main} argument: \code{NULL} means "use the
function's default title", while \code{FALSE}, \code{""} and \code{NA} all mean
"suppress the title" and map to \code{""}.

Like \code{.neededMargin()}, but takes the label orientation (\code{las}) into
account: with perpendicular labels (\code{las} 2/3) the label \emph{width}
governs the required space regardless of the axis side. Adds a 15\%
safety factor to absorb rounding and font metric differences across
devices.
}
\details{
Theme entries whose names start with \code{"group."} are stripped
before merging, as they parameterize grouped-plot variants and are not
valid \code{\link[graphics:grid]{graphics::grid()}} arguments.

This is the outermost wrapper of the shared plot setup path: every
\verb{plot*()} function routes its drawing code through here, so state
restoration and stamping never have to be handled at individual call
sites.

Deliberately \emph{not} saved/restored: \code{oma}/\code{omi}. Restoring these resets
the multi-figure state and thereby destroys user-defined \code{mfrow}/
\code{layout()} arrangements between panels (each \code{par(omi = ...)} call
restarts the page).

Warnings inside \code{expr} are raised immediately (\code{warn = 1}) so they
appear in the context of the failing plot call rather than being
deferred to the end of the top-level call.

The success flag \code{ok} ensures that neither the stamp nor the layout
reset fires when \code{expr} throws: a half-drawn plot is not stamped, and
the (possibly user-owned) layout is left untouched for inspection.

\enumerate{
\item \strong{Theme \code{par}} (\code{getTheme()$par}) -- global styling, lowest tier.
\item \strong{Function defaults} (\code{defaults}), each individually overridable
via the corresponding \verb{DescToolsX.plot.<name>} option (see
\code{.resolvePar()}).
\item \strong{User dots} (\code{...}) -- explicit arguments at the call site,
highest tier.
}

Only names that are settable \code{par()} parameters are applied; anything
else in \code{...} is silently ignored here (it is typically consumed by
the plot primitives instead).

\code{mar} and \code{oma} receive partial-update semantics: a \emph{named} vector
(\code{c(top = 6)}) patches only the given sides (names must be from
\code{bottom}, \code{left}, \code{top}, \code{right}); an unnamed vector is recycled to
length 4, with \code{NA} entries keeping the current value
(\code{mar = c(NA, 8, NA, NA)} widens only the left margin).
}
\section{Why these are exported}{

Section 9.7 of the design rules gives the unavailability of these
helpers outside \code{pharos} as the reason \verb{plot.Desc.*} methods live in
\code{pharos} rather than next to the classes they plot. That reason no
longer holds. The placement of the existing methods is unchanged - the
export only removes the constraint, it does not itself relocate
anything.
}

\section{Contract for callers outside pharos}{

The rules in sections 9.1-9.5 apply unchanged: no direct \code{par()} call
outside \code{.applyParFromDots()}, \code{stamp} passed to
\code{.withGraphicsState()} rather than drawn by hand, no \code{oma}/\code{omi} in any
save/restore list, and no internal \code{mfrow}.
}

\keyword{internal}
